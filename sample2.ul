/*int[] arr = [ 1, 2, 3 ];*/

(name:string, greet:(int)()) greeter =
    (
        name:"Emil",
        age: 2012-1986,
        greet:(int)() {
            print("Hello " + name + "! You are " + intToStr(age) + " this year.");
            return 0;
        }
    );

/*int typeTest = 1+2*3;
int num = arr[2*2-3];*/

/*print(intToStr(greeter.age));*/
print(greeter.name);

greeter.greet();
