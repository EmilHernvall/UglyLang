type Greeter (name:string, setAge:(void)(int), greet:(void)());

Greeter greeter =
    (
        name:"Emil",
        age: 2012-1986,
        setAge:(void)(int v) {
            age = v;
        },
        greet:(void)() {
            print("Hello " + name + "! You are " + intToStr(age) + " this year.");
        }
    );

/*print(intToStr(greeter.age));*/
print(greeter.name);
greeter.greet();

greeter.name = "Torbjörn";
greeter.setAge(52);
greeter.greet();
